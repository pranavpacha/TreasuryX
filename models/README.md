# models/

Reserved for pretrained model weights. **Not used in this build** — the Computer Vision pipeline
uses classical OpenCV techniques + Tesseract OCR rather than a pretrained deep model; see
[../docs/cv_pipeline.md](../docs/cv_pipeline.md) for why. Kept as a placeholder for future work
(see "Future Work" in the root README) rather than removed, per the requested project layout.
